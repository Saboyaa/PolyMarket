# Parallel-agent git worktrees (kept inside repo so sandboxed agents can reach them)
.worktrees/

# Secrets & credentials — NEVER commit
.env
.env.*
!.env.example
*.key
.clob_creds.json
.creds/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Local config with real values
config.toml

# Rolling scan/observation log (analysis data, not source)
scan_log.jsonl
*.jsonl

# Editor / OS
.vscode/
.idea/
.DS_Store
nohup.out
