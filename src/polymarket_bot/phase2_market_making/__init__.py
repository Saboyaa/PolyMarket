"""Phase 2 — market making (NOT YET IMPLEMENTED).

Planned per docs/spec.md: quote both sides of a market, capture the spread, and
manage inventory, earning Polymarket maker rebates. Will reuse the Phase 1
``common/`` layers (config, models, fees, risk, clients, execution).

This package is an intentional stub so the phase boundary is visible in the tree.
"""
